{% trans_default_domain 'netzmacht_workflow' %}

<h2 class="sub_headline">{{ headline }}</h2>

{% if description %}
<div class="workflow-content-box">
    <p>{{ description }}</p>
</div>
{% endif %}

<div class="workflow-content-box">
    <div class="tl_header">
        <table class="tl_header_table">
            <tbody>
            <tr>
                <td><span class="tl_label">{{ 'workflowName'|trans }}</span></td>
                <td>{{ workflow.label }}</td>
            </tr>
            <tr>
                <td><span class="tl_label">{{ 'currentStep'|trans }}</span></td>
                <td>{{ currentStep != null ? currentStep.label : '-' }}</td>
            </tr>
            {% if item.latestState %}
                <tr>
                    <td><span class="tl_label">{{ 'reachedAt'|trans }}</span></td>
                    <td>{{ item.latestState.reachedAt.format('d.m.Y H:i') }}</td>
                </tr>
            {% endif %}
            </tbody>
        </table>
    </div>
</div>
